# Word Similarity Bank

Designed to offer a solution to constrained hardware to still be able to compute similarity and use it to run NLP command parsing without being able to actually run an embedding model.
